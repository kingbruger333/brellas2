meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  skip: false
  additionalCommands: npm run build

run:
  command: npm run start
  containerPort: 3000
  servicePort: 3000
